[project]
name = "ai-examiner"
version = "0.1.0"
description = "AI Examiner Agent (LLM-based exam bot)"
requires-python = ">=3.11"
dependencies = [
    "gradio>=6.1.0",
    "groq>=0.37.1",
]

[tool.uv]
dev-dependencies = []
